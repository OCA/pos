By default, any POS user who tries to open a second active session will receive
a validation error asking them to close the existing session first.

To allow a specific user to bypass this restriction:

1. Go to **Settings > Users & Companies > Users**.
2. Open the user form.
3. Under the **Point of Sale** section, enable **Allow Multiple POS Sessions**.

