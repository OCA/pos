In the Point of Sale, when a product with attributes opens the attribute
selection popup, no product image is shown, even though the popup already
displays the matching product variant.

This module shows the image of the currently selected product variant in
that popup, updating live as the cashier changes attribute values.
