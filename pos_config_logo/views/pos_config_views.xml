<?xml version="1.0" ?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="model">pos.config</field>
        <field name="inherit_id" ref="point_of_sale.pos_config_view_form" />
        <field name="arch" type="xml">
            <div id="receipt" position="inside">
                <div class="col-12 col-lg-6 o_setting_box" id="pos_config_logo_setting">
                    <div class="o_setting_right_pane">
                        <label string="Alternative point of sale logo" for="logo" />
                        <div class="text-muted">
                            If set, this logo will be used instead of the company's one
                        </div>
                        <field name="logo" widget="image" class="w-25 mt-2" />
                    </div>
                </div>
            </div>
        </field>
    </record>
</odoo>
