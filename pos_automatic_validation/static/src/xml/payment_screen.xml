<?xml version="1.0" encoding="UTF-8" ?>
<templates id="template" xml:space="preserve">

    <t
        t-name="pos_automatic_validation.PaymentScreenBack"
        t-inherit="point_of_sale.PaymentScreenBack"
        t-inherit-mode="extension"
    >
        <xpath
            expr="//button[hasclass('button') and hasclass('back')]"
            position="attributes"
        >
            <attribute
                name="t-attf-class"
            >{{ currentOrder.canBeBack() ? '' : 'disabled' }}</attribute>
        </xpath>
        <xpath
            expr="//button[hasclass('btn-switchpane') and hasclass('back-button')]"
            position="attributes"
        >
            <attribute
                name="t-attf-class"
            >{{ currentOrder.canBeBack() ? '' : 'disabled' }}</attribute>
        </xpath>
    </t>

</templates>
