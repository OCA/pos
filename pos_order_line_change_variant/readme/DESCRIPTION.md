Allows changing the product variant of an order line already present in
the Point of Sale cart, without having to remove it and add the new
variant as a separate line.

Click an already-selected line whose product has variants to reopen
the variant configurator, pre-filled with the line's current attribute
values.
