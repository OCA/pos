No additional configuration is required. The PWA is enabled automatically
when the module is installed. The Service Worker registers on the first
visit to `/pos/ui`.

To install the POS as a standalone app, open it in Chrome or Safari and
use the browser's "Install" or "Add to Home Screen" option.
