<?xml version="1.0" encoding="UTF-8" ?>
<templates id="template" xml:space="preserve">
    <t
        t-name="Orderline"
        t-inherit="point_of_sale.Orderline"
        t-inherit-mode="extension"
        owl="1"
    >
        <xpath expr="//ul[hasclass('info-list')]" position="inside">
            <t t-if="props.line.combo_rule_id and props.line.combo_discount_amount">
                <li class="info">
                    With
                    <em>
                        <t
                            t-esc="env.pos.format_currency(props.line.combo_discount_amount, 'Product Price')"
                        />
                    </em>
                    discount from combo.
                </li>
            </t>
        </xpath>
    </t>
</templates>
